# Logical Data Schema

Concrete logical representation of the locked Information Model and the locked Operational Specification.

- `V0.1/` — locked Logical Data Schema V0.1.

Operational classifications, provenance metadata and process records (independence state, primary_event_identity, upstream_identity, confounder-check metadata, materiality metadata, exploration classification) are carriers stored in designated fields. They are **not** first-class Information Model entities.
