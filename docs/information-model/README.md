# Information Model

Normative information-model specifications.

- `V0.1/` — locked Information Model V0.1.

The Information Model is the semantic contract. The Logical Data Schema must implement it without changing its meaning. Any semantic change requires an explicit new Information Model version.
